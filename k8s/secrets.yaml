# Optional: Create secrets for sensitive data
# Note: Values should be base64 encoded
# Example: echo -n "your-secret-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: finpulse-app-secrets
  namespace: default
type: Opaque
data:
  # Add your secrets here (base64 encoded)
  # Example:
  # API_KEY: eW91ci1hcGkta2V5LWhlcmU=
